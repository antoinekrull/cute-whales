CREATE TABLE IF NOT EXISTS deaths_and_temperature (
                            year VARCHAR,
                            month VARCHAR,
                            region VARCHAR,
                            totaldeaths VARCHAR,
                            temperature DOUBLE PRECISION
                        );